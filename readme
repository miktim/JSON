# my JSON package, MIT (c) 2020 miktim@mail.ru (invented the wheel?)

Release notes:
  - java 1.7+, Android compatible;
  - in accordance with RFC 8259: https://datatracker.ietf.org/doc/rfc8259/?include_text=1
  - supported java objects:
      JSON object, String, Number, Boolean, null, Object[] array of listed types;
  - parser implements BigDecimal for numbers.
  
Overview:
    static Object parse(String json) throws IOException;
    static Object parse(Reader reader) throws IOException;
    static String stringify(Object object) throws IllegalArgumentException;

  Constructor:
    JSON();

  Methods:
    String stringify();
    String toString();
    JSON clone() throws CloneNotSupportedException;
    List<String> list();
    boolean exists(String propName);
    Object get(String propName) throws IllegalArgumentException;
    JSON set(String propName, Object value) throws IllegalArgumentException;
    Object remove(String propName)
    
Usage example see in https://github.com/miktim/JSON/blob/master/test/JSONTest.java